import { createTheme } from '@mui/material';

const theme = createTheme({
  palette: {
    primary: {
      main: '#757575',
    },
    secondary: {
      main: '#01579b',
    },
  },
});

export default theme;
